import { NavSelectedDirective } from './nav-selected.directive';

describe('NavSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new NavSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
